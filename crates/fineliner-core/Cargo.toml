[package]
name = "fineliner-core"
version = "0.1.0"
description = "Pure logic core for Pincel's Image mode (formerly the Fineliner image editor): document model, commands, rendering, codecs."
# Imported from amigo-fineliner@01eef70 at edition 2021; the 2024 flip is a
# separate commit.
edition = "2021"
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
uuid = { workspace = true }
thiserror = { workspace = true }
image = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
ab_glyph = { workspace = true }

[dev-dependencies]
insta = "1"
proptest = "1"

[lints]
workspace = true
